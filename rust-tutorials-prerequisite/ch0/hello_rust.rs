// Rust doesn't have garbage collection, uses a concept called "ownership and borrowing"
// Package manager is called "Cargo", similar to NPM for node

fn main(){
    println!("Hello, 🦀!");
}

// to activate, ./hello_rust
// must use two commands if not using cargo
// rustc and ./func

